{
  "name": "todo-list-app",
  "version": "1.0.0",
  "description": "Browser-based todo list app",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "fast-check": "^4.8.0",
    "vitest": "^2.1.9"
  },
  "vitest": {
    "include": [
      "test.js"
    ]
  }
}
